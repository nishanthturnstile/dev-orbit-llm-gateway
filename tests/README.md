# Tests

This directory is reserved for future smoke and policy tests.

Phase 1 creates the tracked test home only. Smoke test skeletons and test implementation are deferred to Phase 2 and later phases.

Future tests should avoid committing prompts, responses, provider keys, generated virtual keys, database URLs, Railway variables, private hostnames, or backup credentials.
