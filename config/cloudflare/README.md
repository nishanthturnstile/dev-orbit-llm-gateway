# Cloudflare configuration notes

Cloudflare Tunnel, Access, WAF, or edge origin-guard configuration is deferred from the v1 baseline unless later approved.

This directory exists only as a documentation home for future hardening decisions.

Do not store Cloudflare tunnel tokens, service-token secrets, account IDs, private origins, or generated policy credentials in source control.
